// eslint.config.mjs
export default {
  // Tell ESLint this is the root config, so it doesn't merge with anything else.
  root: true,

  // If you want to ignore certain files anyway:
  // ignores: ['some-file-or-directory'],

  // No parser plugins, no recommended configs, no rules
  rules: {}
};
